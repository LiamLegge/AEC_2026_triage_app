import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

// NOTE: Comments in this file reflect AI-assisted coding directed by Jackson Chambers.
// Create the React 18 root and mount the application — AI-assisted coding directed by Jackson Chambers.
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    {/* StrictMode helps catch issues in development — AI-assisted coding directed by Jackson Chambers. */}
    <App />
  </React.StrictMode>
);
